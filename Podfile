# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'
use_frameworks!

def all_pods
  
  # Progress HUD
  pod 'MBProgressHUD'
  
  # Google
  pod 'GoogleMaps'
end

target 'PhotosApp' do
  all_pods
end

target 'PhotosAppTests' do
  all_pods
end
